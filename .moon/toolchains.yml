rust:
  syncToolchainConfig: true
  components:
    - llvm-tools-preview
    - rustfmt
    - clippy
    - rust-analyzer
    - rust-src
  bins:
    - cargo-nextest
    - cargo-hakari
    - cargo-llvm-cov
    - diesel_cli

javascript:
  packageManager: 'pnpm'
  syncProjectWorkspaceDependencies: true
  syncPackageManagerField: true

node:
  version: '24'

pnpm:
  version: "10"
