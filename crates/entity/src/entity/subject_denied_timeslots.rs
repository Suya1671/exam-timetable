//! `SeaORM` Entity, @generated by sea-orm-codegen 1.1.19

use crate::id::{SubjectId, TimeslotId};
use sea_orm::entity::prelude::*;

#[sea_orm::model]
#[derive(Clone, Debug, PartialEq, DeriveEntityModel, Eq)]
#[sea_orm(table_name = "subject_denied_timeslots")]
pub struct Model {
    #[sea_orm(primary_key, auto_increment = false)]
    pub subject_id: SubjectId,
    #[sea_orm(primary_key, auto_increment = false)]
    pub timeslot_id: TimeslotId,

    #[sea_orm(
        belongs_to,
        from = "subject_id",
        to = "id",
        on_update = "Cascade",
        on_delete = "Cascade"
    )]
    pub subject: HasOne<super::subjects::Entity>,

    #[sea_orm(
        belongs_to,
        from = "timeslot_id",
        to = "id",
        on_update = "Cascade",
        on_delete = "Cascade"
    )]
    pub timeslot: HasOne<super::timeslots::Entity>,
}

impl ActiveModelBehavior for ActiveModel {}
