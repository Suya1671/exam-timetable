[package]
name = "backend"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
model = { version = "0.1.0", path = "../model" }
solver = { version = "0.1.0", path = "../solver" }
hack = { version = "0.1.0", path = "../hack" }
sqlx.workspace = true
futures = "0.3.32"
thiserror = "2.0.18"
time = "0.3.47"
itertools = "0.14.0"

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
