[package]
name = "solver"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
model = { version = "0.1.0", path = "../model" }
z3 = "0.19.10"
hack = { version = "0.1", path = "../hack" }
thiserror = "2.0.18"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
